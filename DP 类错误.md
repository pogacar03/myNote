# DP 类错误

## 1. LC198 打家劫舍：`dp[1]` 初始化错误

### 错误写法

```java
dp[0] = nums[0];
dp[1] = nums[1];
```

### 正确写法

```java
dp[0] = nums[0];
dp[1] = Math.max(nums[0], nums[1]);
```

### 为什么

`dp[i]` 表示考虑到第 `i` 间房时，前 `i + 1` 间房能够偷到的最大金额。

因此 `dp[1]` 不是“第二间房的钱”，而是“前两间房最多能偷多少钱”。由于相邻房屋不能同时偷，所以前两间只能二选一：

```java
dp[1] = Math.max(nums[0], nums[1]);
```

例如：

```text
nums = [5, 1]
```

如果写成 `dp[1] = nums[1] = 1`，就会丢掉第一间房的最优答案 `5`。

### 转移方程

```java
dp[i] = Math.max(dp[i - 1], dp[i - 2] + nums[i]);
```

- 不偷第 `i` 间：`dp[i - 1]`
- 偷第 `i` 间：`dp[i - 2] + nums[i]`

### 速记

> DP 初始化也必须符合 `dp` 的定义。`dp[1]` 表示前两间房的最优解，不等于 `nums[1]`。
